# what about languages?
interface BaseObject {
  id: ID!
  title: String!
  status: String
  created: AWSDateTime
  updated: AWSDateTime
}

input S3ObjectInput {
  bucket: String!
  key: String!
  region: String!
}

type S3Object {
  bucket: String
  key: String
  region: String
}

enum LangEnum {
    fr
    en
}

