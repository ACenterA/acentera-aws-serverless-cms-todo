// import AWS from 'aws-sdk'

const apollo = {
  state: {
  },
  mutations: {
    /*
    SET_AMI: (state, data) => {
      state.ami = data
    }
    */
  },
  getters: {
    /*
    cluster: state => {
      return state.cluster
    },
    ami: state => {
      return state.ami
    }
    */
  },
  actions: {
    GET_APOLLO({ commit, state }, data) {
    }
  }
}

export default apollo
